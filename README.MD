---

## 📑 สารบัญ (Table of Contents)
- [🏗️ โครงสร้าง Network และ Port ใน Docker Compose](#️-โครงสร้าง-network-และ-port-ใน-docker-compose)
  - [1. การบริหารจัดการ Network (Network Management)](#1-การบริหารจัดการ-network-network-management)
    - [🔒 จุดเด่นด้านความปลอดภัย:](#-จุดเด่นด้านความปลอดภัย)
  - [2. การกำหนดพอร์ต (Port Mapping)](#2-การกำหนดพอร์ต-port-mapping)
  - [3. การสื่อสารภายใน (Internal Communication)](#3-การสื่อสารภายใน-internal-communication)
  - [🛠️ ข้อแนะนำเพิ่มเติม](#️-ข้อแนะนำเพิ่มเติม)

# 🐳 Docker Compose Network & Port Configuration

โปรเจกต์นี้เป็นการตั้งค่าระบบแบบ Multi-tier ที่แบ่งแยก Network เพื่อความปลอดภัย และแสดงผลผ่าน Portainer Dashboard

## 📄 1. ไฟล์ Docker Compose (`docker-compose.yml`)

```yaml
services:
  frontend:
    image: nginx:alpine
    container_name: web-frontend
    ports:
      - "8086:80"
    networks:
      - frontend-net
      - shared-net

  backend:
    image: cloudnativelabs/whats-my-ip
    container_name: api-backend
    ports:
      - "3001:8080"
    networks:
      - backend-net
      - shared-net

  database:
    image: postgres:alpine
    container_name: db-service
    ports:
      - "5433:5432"
    environment:
      POSTGRES_PASSWORD: password
    networks:
      - backend-net

  portainer:
    image: portainer/portainer-ce:latest
    container_name: docker-manager
    ports:
      - "9000:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    networks:
      - frontend-net
      - shared-net
      - backend-net

volumes:
  portainer_data:

networks:
  frontend-net:
  backend-net:
  shared-net:
    driver: bridge
```

## 🚀 2. ขั้นตอนการเตรียมเครื่องและรัน (Execution Guide)
###  ✅ ขั้นเตรียมการ (Prerequisites)
ติดตั้ง Docker Desktop (สำหรับ Windows/Mac) หรือ Docker Engine (สำหรับ Linux)
ตรวจสอบเวอร์ชันด้วยคำสั่ง docker compose version
###   🛠️ คำสั่งที่ใช้รัน
ให้เปิด Terminal ในโฟลเดอร์ที่เก็บไฟล์ไว้ แล้วพิมพ์คำสั่งดังนี้:
### เริ่มการทำงาน:
```bash
docker compose up -d
```

###  ตรวจสอบสถานะ Container:
```bash
docker compose ps
```

### ตรวจสอบ Network ที่ถูกสร้างขึ้น:
```bash
docker network ls
```

## 🌐 3. การเข้าใช้งาน (Access URLs)
### Service	URL	คำอธิบาย
* Frontend	http://localhost:8086	หน้าเว็บหลัก (Nginx)
* Backend API	http://localhost:3001	เช็คสถานะ API และ 
IP ภายใน
* Portainer	http://localhost:9000	Dashboard สำหรับดู Network และ Container

## ⚙️ 4. คำสั่งจัดการเพิ่มเติม (Maintenance)
* หยุดการทำงานชั่วคราว: 
 ```bash
docker compose stop
 ```
* เริ่มการทำงานใหม่: 
```bash 
docker compose start
```

* ลบระบบทิ้งทั้งหมด (รวม Network): 
 ```bash 
docker compose down
```

* ดู Log การทำงาน: 
```bash  
docker compose logs -f
```

#### คำแนะนำด้านความปลอดภัย: สำหรับพอร์ต 5433 ของ Database หากไม่ได้ใช้ต่อจากภายนอก แนะนำให้เอาส่วน ports ออก เพื่อให้เข้าถึงได้เฉพาะจาก backend-net เท่านั้น ตามหลักการ Docker Networking Best Practices

---


# 🏗️ โครงสร้าง Network และ Port ใน Docker Compose

ไฟล์นี้มีการออกแบบระบบเครือข่ายแบบ **Multi-tier Architecture** เพื่อแยกส่วนการทำงานและความปลอดภัยออกจากกันอย่างเป็นระบบ

---

## 1. การบริหารจัดการ Network (Network Management)

มีการแบ่ง Network ออกเป็น 3 วงหลัก เพื่อกำหนดขอบเขตความปลอดภัย (**Isolation**):


| ชื่อ Network | หน้าที่หลัก | Service ที่เชื่อมต่ออยู่ |
| :--- | :--- | :--- |
| **frontend-net** | รับ Traffic จากภายนอกหรือส่วนหน้าบ้าน | `frontend`, `portainer` |
| **backend-net** | ส่วนประมวลผลหลังบ้านและฐานข้อมูล (ปลอดภัยที่สุด) | `backend`, `database`, `portainer` |
| **shared-net** | **"สะพานเชื่อม"** ให้หน้าบ้านคุยกับหลังบ้านได้ | `frontend`, `backend`, `portainer` |

### 🔒 จุดเด่นด้านความปลอดภัย:
*   **Database Isolation:** `database` อยู่ใน `backend-net` เท่านั้น ทำให้ `frontend` **ไม่สามารถ** เชื่อมต่อกับฐานข้อมูลโดยตรงได้ (ป้องกันการเจาะข้อมูลจากหน้าเว็บ)
*   **Controlled Access:** `frontend` จะคุยกับ `backend` ได้เฉพาะผ่านวง `shared-net` เท่านั้น

---

## 2. การกำหนดพอร์ต (Port Mapping)

การเชื่อมโยงพอร์ตระหว่าง **เครื่อง Host (คอมพิวเตอร์ของคุณ)** และ **Container** มีรายละเอียดดังนี้:


| Service | Port (Host:Container) | คำอธิบายการเข้าถึง |
| :--- | :--- | :--- |
| **Frontend** | `8086:80` | [http://localhost:8086](http://localhost:8086) |
| **Backend** | `3001:8080` | [http://localhost:3001](http://localhost:3001) |
| **Database** | `5433:5432` | ต่อผ่านโปรแกรมภายนอก (เช่น DBeaver) พอร์ต `5433` |
| **Portainer** | `9000:9000` | [http://localhost:9000](http://localhost:9000) |

---

## 3. การสื่อสารภายใน (Internal Communication)

แม้ภายนอกจะเข้าผ่านพอร์ต `8086` หรือ `3001` แต่ภายใน Docker Network พวกมันคุยกันผ่าน **Service Name** และ **Container Port** โดยตรง:

*   **Frontend คุยกับ Backend:** เรียกผ่าน `http://api-backend:8080`
*   **Backend คุยกับ Database:** เรียกผ่าน `http://db-service:5432`
*   **DNS:** [Docker Embedded DNS](https://docs.docker.com) จะคอยแปลงชื่อ `container_name` เป็น IP Address ภายในให้โดยอัตโนมัติ

---

## 🛠️ ข้อแนะนำเพิ่มเติม

*   **Driver:** ใน `shared-net` ระบุเป็น `driver: bridge` ซึ่งถูกต้องแล้วสำหรับการรันบนเครื่องเดียวตาม [Docker Network Documentation](https://docs.docker.com)
*   **Volumes:** มีการประกาศ `portainer_data` เพื่อเก็บข้อมูลการตั้งค่าของ [Portainer](https://docs.portainer.io) ไม่ให้หายไปเมื่อลบ Container ตามหลักการ **Persistence Data**
